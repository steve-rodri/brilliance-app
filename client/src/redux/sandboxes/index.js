import Sandbox from "./Sandbox";
export const eventSandbox = new Sandbox({
  list: true,
  form: true,
  search: true
});
export const invoiceSandbox = new Sandbox({
  list: true,
  form: true,
  search: true
});
export const scheduleSandbox = new Sandbox({ list: true });
